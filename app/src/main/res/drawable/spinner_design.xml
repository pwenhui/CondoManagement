<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <solid android:color="#FFFFFF" />

    <!-- Corner radius -->
    <corners android:radius="12dp" />

    <corners android:padding="12dp" />

    <!-- Border -->
    <stroke
        android:width="2dp"
        android:color="@color/black" />
</shape>
